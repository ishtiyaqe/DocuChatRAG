server {
  listen 5173;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri /index.html;
  }

  # proxy to backend if needed (not used; FE calls BACKEND_URL directly)
  # location /api/ { proxy_pass http://backend:8000/api/; }
}
